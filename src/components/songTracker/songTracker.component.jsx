import './songTracker.styles.css'

import { concerts } from '../../data'

const SongTracker = () => {
//   const songCount = locations.reduce((acc, curr) => {}, [])
console.log(concerts)
  return (
    <div>
      <h1>songs</h1>
      {/* <p>{songCount}</p> */}
    </div>
  )
}

export default SongTracker
